import styled from "@emotion/styled";
import { Tooltip } from "@mui/material";

export const StledTooltip = styled(Tooltip)({
    cursor: 'pointer',
    padding: '10px',
    border: '1px solid #000',
    borderRadius: '10px',
    marginRight: '5px',
    fontSize: '12px'
})